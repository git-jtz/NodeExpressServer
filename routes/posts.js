var express = require('express');
var router = express.Router();

/* GET post page. */
router.post('/posts', function(req, res) {
  //res.render('index', { title: 'Express', layout: '_layout' });
});
module.exports = router;
